// ==========================================================================
// Supabase connection settings for the Motos America Sales Academy.
//
// HOW TO FILL THIS IN (one-time setup):
//   1. Go to https://supabase.com and create a free project.
//   2. In your project, go to: Project Settings -> API
//   3. Copy the "Project URL" and paste it below as SUPABASE_URL.
//   4. Copy the "anon public" key and paste it below as SUPABASE_ANON_KEY.
//      (Do NOT use the "service_role" key here — that one is secret and
//      must never appear in a public website.)
//   5. Run schema.sql once in the Supabase SQL Editor to create the tables.
//
// This "anon" key is SAFE to publish in a public repo — it only allows the
// specific actions we've enabled via Row Level Security in schema.sql.
// ==========================================================================

window.SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL_HERE";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY_HERE";
